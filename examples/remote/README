RemoteWebDriver Example
=======================

Download the standalone Selenium server (selenium-server-standalone-*.jar) from
http://code.google.com/p/selenium/downloads/list?q=label:Featured

Compile the example with:

    javac -classpath "selenium-server-standalone-*.jar:." RemoteWebDriverExample.java

Start the server and run the client:

    java -jar selenium-server-standalone-*.jar
    java  -classpath "selenium-server-standalone-*.jar:." RemoteWebDriverExample

Replace the *s with the version number. On Windows replace ":" with ";".